import DataLoader from "./data-loader";

export function FSLoader() {
    return (
        <div className="w-full h-screen flex items-center justify-center flex-col"><div><DataLoader /></div></div>
    );
}